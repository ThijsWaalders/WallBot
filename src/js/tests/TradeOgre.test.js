// Import the TradeOgre functions
const toTest = require('../TradeOgre');

test('Should sort table data rows', n => {
	expect(toTest.sortTable(n)).toBe(n);
});

describe('Arbitrage Check', => {
	it('Should check for currency pair buy price', => {
		expect(curPrice.)
	})
})